# MNIST-Detect-Single-Digit-Object-Detection-with-TensorFlow-Keras
A compact CNN that jointly classifies and localizes a single MNIST digit randomly placed on a 64×64 canvas — a minimal, self-contained object detection pipeline (synthetic data generation → multi-task CNN → IoU evaluation → visualization) built with no external annotation tools.
